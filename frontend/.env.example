# 프론트 빌드/실행 시 주입하는 환경변수. 로컬 개발은 비워도 기본값(localhost:8000)으로 동작한다.
# 배포 시 아래를 실제 백엔드 주소로 채운다.
VITE_API_BASE=http://localhost:8000
# 미지정 시 VITE_API_BASE에서 http→ws로 자동 유도되므로 보통 생략 가능.
# VITE_WS_BASE=ws://localhost:8000
